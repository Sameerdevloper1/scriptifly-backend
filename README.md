# Scriptifly Backend

A lightweight Flask API for retrieving YouTube transcript text for the Scriptifly Android application.

The backend does not run speech-to-text or AI models.

It retrieves transcripts that YouTube makes available through `youtube-transcript-api`.

## Requirements

- Python 3.10+
- pip
- Internet access

## Installation

Create a virtual environment:

```bash
python -m venv .venv